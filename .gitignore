# Secrets
.env
.env.*
!.env.example

# Dependencies
node_modules/
**/node_modules/
venv/
.venv/
**/.venv/
tools/sharepoint-mcp-venv/
.xlsx_venv/
**/.xlsx_venv/

# Python
__pycache__/
*.py[cod]
.Python
*.egg-info/
.eggs/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Build / caches
dist/
build/
.cache/
coverage/
.pytest_cache/

# Logs
*.log

# Agent run outputs (keep .gitkeep placeholders)
data/runs/**/*.json
data/runs/**/*.md
data/runs/**/*.csv
!data/runs/**/.gitkeep

# mgm-network-profile agent output
output/
agents/mgm-network-profile/output/

# Local runtime (SDM Webex)
sdm-files/subscribers.json

# Optional: large local archives (uncomment if repo should stay smaller)
# _archived/

# Comms queue (transient — keep completed/ for audit)
.comms/inbox/
.comms/outbox/
.comms/active/
